dotfiles管理用のリポジトリ
シンボリックリンクを張っているので、このリポジトリ以下のファイルを更新すると即座に反映される

# minipcブランチについて
このブランチは「検証環境minipc専用の最低限dotfiles」ブランチ。
mainには絶対にマージしない。mainと混同しないこと。

minipcはWSL上のWindows Terminalから接続するLinux nativeサーバで、
主にClaude Code検証とtmux作業に使う。
プロンプトは [minipc] 赤ラベルで普段環境と区別している。
